// Brand / contact configuration — single source of truth.
//
// SUPPORT_EMAIL is referenced by /privacy and /terms. Update it here when the
// real brand address is ready (Nico is setting one up); it's the only place
// to change.
export const BRAND = "Tarima";
export const SUPPORT_EMAIL = "hola@tarima.app"; // TODO: replace with the real support address
